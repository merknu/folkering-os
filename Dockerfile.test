FROM ubuntu:22.04

# Install dependencies
RUN apt-get update && apt-get install -y \
    qemu-system-x86 \
    xorriso \
    mtools \
    dosfstools \
    && rm -rf /var/lib/apt/lists/*

# Set working directory
WORKDIR /test

# Entry point will run QEMU
ENTRYPOINT ["qemu-system-x86_64"]
